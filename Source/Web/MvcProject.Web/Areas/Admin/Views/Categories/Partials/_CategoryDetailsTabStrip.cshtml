@(Html.TabStripHelper(
    "tabStrip_#=Id#",
    0,
    items =>
    {
        items.Add().Text("Products").Content(@<text>@Html.Partial("_ProductsTab")</text>);
    })
    .ToClientTemplate()
    .ToMvcClientTemplate())

