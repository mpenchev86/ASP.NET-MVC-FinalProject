@model ICollection<string>

@{ 
    foreach (var item in Model)
    {
        @Html.ActionLink(item, "AllProductsWithTag", "Search", new { tag = item }, new { @class = "btn btn-default" })
    }
}