@using MvcProject.Web.Areas.Public.ViewModels.Categories

@model List<CategoryForLayoutDropDown>

<a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Categories<span class="caret"></span></a>
<ul class="dropdown-menu">
    @for (int i = 0; i < Model.Count(); i++)
    {
        @*<li>@Html.ActionLink(Model[i].Name, "SearchByCategory", "Search", new { categoryId = Model[i].Id, query = string.Empty, area = Areas.PublicAreaName }, null)</li>*@
        <li>@Html.ActionLink(Model[i].Name, "CategorySearchResult", "Search", new { categoryId = Model[i].Id, query = string.Empty, area = Areas.PublicAreaName }, null)</li>
    }
</ul>