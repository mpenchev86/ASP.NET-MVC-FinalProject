@using MvcProject.Web.Areas.Administration.ViewModels.Images

@*@(Html.Kendo().Upload()
    .Name((string)ViewData["Name"])
    .Enable((bool)ViewData["Enable"])
    .Multiple((bool)ViewData["Multiple"])
    .Async(async => async
        .AutoUpload((bool)ViewData["AutoUpload"])
        .Batch((bool)ViewData["Batch"])
        //.SaveField((string)ViewData["Name"])
        .Save((string)ViewData["SaveAction"], (string)ViewData["SaveController"], ViewData["routeValues"])
        .Remove((string)ViewData["RemoveAction"], (string)ViewData["RemoveController"], ViewData["routeValues"])
    )
    .Files(f => f.Add().Name("pesho").Extension(".ppj"))
    //.Files(cfg =>
    //{
    //    var files = (IEnumerable<ImageDetailsForProductViewModel>)ViewData["InitialFiles"];
    //    foreach (var file in files)
    //    {
    //        cfg.Add().Name(file.OriginalFileName).Extension(file.FileExtension);
    //    }
    //})
    //.ShowFileList(false)
    .Events(e => e.Success((string)ViewData["SuccessEventHandler"]))
//.Messages(msg => msg.Remove("re-move"))
//.TemplateId("imagesTemplate")
//.HtmlAttributes(new { data_bind = (string)ViewData["data_bind"] })
)*@

@*@model string*@

@Html.HiddenFor(m => m)
@Html.ValidationMessageFor(m => m)

<input id="productImages" name="productImages[]" type="file" />
