@(Html.TabStripHelper(
    "tabStrip_#=Id#",
    0,
    items =>
    {
        items.Add().Text("Users").Content(@<text>@Html.Partial("_UsersTab")</text>);
    })
    .ToClientTemplate()
    .ToMvcClientTemplate())
