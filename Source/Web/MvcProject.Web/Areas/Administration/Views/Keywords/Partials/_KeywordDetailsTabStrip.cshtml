@(Html.TabStripHelper(
    "tabStrip_#=Id#",
    0,
    items =>
    {
        items.Add().Text("Categories").Content(@<text>@Html.Partial("_CategoriesTab")</text>);
    })
    .ToClientTemplate()
    .ToMvcClientTemplate())
